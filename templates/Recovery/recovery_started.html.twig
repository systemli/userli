{% extends 'Recovery/recovery.html.twig' %}

{% block recovery_content %}
    <div class="mb-6 bg-green-50 border border-green-200 rounded-lg p-4">
        <div class="flex">
            <svg class="w-5 h-5 text-green-500 mt-0.5 mr-3" fill="currentColor" viewBox="0 0 20 20" aria-hidden="true">
                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
            </svg>
            <div class="text-sm text-green-800">
                <p>{{ "recovery.started"|trans }}</p>
                <p class="mt-2">{{ "recovery.waiting-info"|trans }}</p>
            </div>
        </div>
    </div>

    <div class="bg-blue-50 border border-blue-200 rounded-lg p-4">
        <div class="flex">
            <svg class="w-5 h-5 text-blue-500 mt-0.5 mr-3" fill="currentColor" viewBox="0 0 20 20" aria-hidden="true">
                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zm8-5a.5.5 0 00-1 0v4.5a.5.5 0 00.5.5h3a.5.5 0 000-1h-2.5V9a.5.5 0 00-.5-.5z" clip-rule="evenodd"></path>
            </svg>
            <div class="text-sm font-medium text-blue-800">
                {{ "recovery.waiting-time"|trans({'%time%': active_time|date("d.m.Y")}) }}
            </div>
        </div>
    </div>

    <div class="mt-6 flex justify-center">
        <a href="{{ url('index') }}"
           class="px-6 py-3 bg-blue-600 text-white font-medium rounded-lg hover:bg-blue-700 focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 transition-colors duration-200">
            {{ "error.back_link"|trans }}
        </a>
    </div>
{% endblock %}
