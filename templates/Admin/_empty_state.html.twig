{# Parameters: icon (e.g. 'heroicons:key'), title_key, description_key #}
<div class="text-center py-12">
    {{ ux_icon(icon, {class: 'w-16 h-16 mx-auto mb-4 text-gray-300 dark:text-gray-600'}) }}
    <h3 class="text-lg font-medium text-gray-900 dark:text-gray-100 mb-2">{{ title_key|trans }}</h3>
    <p class="text-gray-500 dark:text-gray-300 max-w-md mx-auto">{{ description_key|trans }}</p>
</div>
