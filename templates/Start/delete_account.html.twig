<div class="flex items-center mb-4">
    <div class="w-12 h-12 bg-red-100 rounded-xl flex items-center justify-center mr-4">
        {{ ux_icon('heroicons:trash', {class: 'w-6 h-6 text-red-600'}) }}
    </div>
    <h2 class="text-xl font-semibold text-gray-900">{{ "index.delete-headline"|trans }}</h2>
</div>

<p class="text-gray-600 mb-6">{{ "index.delete-description"|trans({'%project_name%': project_name})|raw }}</p>

<div class="flex justify-center sm:justify-start">
    <a href="{{ url('user_delete') }}"
       class="px-5 py-2.5 bg-red-600 text-white font-medium rounded-lg hover:bg-red-700 focus:ring-4 focus:ring-red-300 focus:outline-none transition-colors duration-200"
       aria-label="{{ "index.delete-button"|trans }}"
       role="button">
        {{ "index.delete-button"|trans }}
    </a>
</div>
