<div class="flex items-center mb-6">
    <div class="w-12 h-12 bg-purple-100 rounded-xl flex items-center justify-center mr-4">
        {{ ux_icon('heroicons:lock-closed', {class: 'w-6 h-6 text-purple-600'}) }}
    </div>
    <h2 class="text-xl font-semibold text-gray-900">{{ "account.twofactor.headline"|trans }}</h2>
</div>

<p class="text-gray-600 mb-4">{{ "account.twofactor.lead"|trans }}</p>

{% if not user.totpAuthenticationEnabled %}
    <div class="bg-yellow-50 border border-yellow-200 rounded-lg p-4 mb-6">
        <div class="flex">
            {{ ux_icon('heroicons:exclamation-triangle-20-solid', {class: 'w-5 h-5 text-yellow-400 mt-0.5 mr-3'}) }}
            <div class="text-sm text-yellow-800">
                {{ "account.twofactor.unset"|trans }}
            </div>
        </div>
    </div>
{% else %}
    <div class="bg-green-50 border border-green-200 rounded-lg p-4 mb-6">
        <div class="flex">
            {{ ux_icon('heroicons:check-circle-20-solid', {class: 'w-5 h-5 text-green-500 mt-0.5 mr-3'}) }}
            </svg>
            <div class="text-sm text-green-800">
                {{ "account.twofactor.set"|trans }}
            </div>
        </div>
    </div>
{% endif %}

<a href="{{ url('user_twofactor') }}" class="inline-flex items-center px-6 py-3 bg-purple-600 text-white font-medium rounded-lg hover:bg-purple-700 focus:ring-2 focus:ring-purple-500 focus:ring-offset-2 transition-colors duration-200">
    {{ ux_icon('heroicons:cog', {'class': 'w-4 h-4 mr-2'}) }}
    {{ "account.twofactor.button"|trans }}
</a>
