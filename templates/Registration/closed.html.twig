{% extends 'base.html.twig' %}

{% block subtitle %}{{ "closed.title"|trans }}{% endblock %}

{% block content %}
    <div class="row">
        <div class="col-xs-12 col-sm-8 col-md-6 col-sm-offset-2 col-md-offset-3">
            <h2>{{ "closed.headline"|trans }}</h2>

            <p class="lead">{{ "closed.lead"|trans }}</p>

            {{ "closed.text"|trans|raw }}
        </div>
    </div>
{% endblock %}
