<?xml version="1.0"?>
<psalm
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns="https://getpsalm.org/schema/config"
    xsi:schemaLocation="https://getpsalm.org/schema/config vendor/vimeo/psalm/config.xsd"
    errorLevel="5"
    resolveFromConfigFile="true"
    findUnusedCode="false"
    phpVersion="8.4"
>
    <projectFiles>
        <directory name="src" />
        <ignoreFiles>
            <directory name="vendor" />
            <directory name="var" />
            <directory name="tests" />
        </ignoreFiles>
    </projectFiles>

    <plugins>
        <pluginClass class="Psalm\SymfonyPsalmPlugin\Plugin" />
    </plugins>

    <issueHandlers>
        <!--
            Symfony Psalm Plugin Issues
            ============================
            QueryBuilderSetParameter: False positives from the Symfony plugin
            when using DQL parameters. These are safe to suppress.
        -->
        <PluginIssue name="QueryBuilderSetParameter" errorLevel="suppress" />

        <!--
            ClassMustBeFinal - Doctrine Entity Exception
            =============================================
            Doctrine entities cannot be final because Doctrine needs to create
            lazy loading proxy classes that extend the entity classes.

            Suppressing ClassMustBeFinal for the entire Entity directory.
        -->
        <ClassMustBeFinal>
            <errorLevel type="suppress">
                <directory name="src/Entity" />
            </errorLevel>
        </ClassMustBeFinal>
    </issueHandlers>
</psalm>
